# Jarvis OS Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a Jarvis OS button and first usable `/jarvis-os` desktop widget shell powered by local Qwen 3.6 27B-first state.

**Architecture:** Reuse the existing `brain_server.py` static web server and `jarvis_web` assets. Add one static Jarvis OS page, one small JSON state endpoint, and one Operations Center launcher button. Keep CSS/JS isolated inside `jarvis-os.html` so existing Operations Center layout is not destabilised.

**Tech Stack:** Python `http.server` handler in `src/openjarvis/cli/brain_server.py`, static HTML/CSS/vanilla JS in `jarvis_web`, pytest static/route tests, manual/browser screenshot verification.

---

## File Structure

- Modify `src/openjarvis/cli/brain_server.py`
  - Route `/jarvis-os` and `/jarvis-os.html` to `jarvis_web/jarvis-os.html`.
  - Add `/jarvis-os/state` JSON endpoint.
  - Add helper `_jarvis_os_state()` near the other page helper functions.
- Create `jarvis_web/jarvis-os.html`
  - Full-screen desktop-first widget shell.
  - Isolated CSS and JavaScript.
  - Fetches `/jarvis-os/state` for model, mission, agent, plugin, market, GPU, schedule, inbox, and memory widgets.
- Modify `jarvis_web/brain.html`
  - Add top-banner Jarvis OS launch button.
  - Link to `/jarvis-os`.
- Create `tests/web/test_jarvis_os.py`
  - Static tests for the page structure and safe rendering helpers.
  - Route source tests for `/jarvis-os` and `/jarvis-os/state`.

---

### Task 1: Route And Launcher

**Files:**
- Modify: `src/openjarvis/cli/brain_server.py`
- Modify: `jarvis_web/brain.html`
- Create: `tests/web/test_jarvis_os.py`

- [ ] **Step 1: Write failing route and launcher tests**

Create `tests/web/test_jarvis_os.py` with:

```python
from pathlib import Path


ROOT = Path(__file__).resolve().parents[2]


def test_brain_operations_center_links_to_jarvis_os():
    html = (ROOT / "jarvis_web" / "brain.html").read_text(encoding="utf-8")

    assert 'id="ob-jarvis-os-btn"' in html
    assert "window.location='/jarvis-os'" in html
    assert ">Jarvis OS<" in html


def test_brain_server_resolves_jarvis_os_static_file():
    from openjarvis.cli.brain_server import _jarvis_web_path

    assert _jarvis_web_path("jarvis-os.html") == ROOT / "jarvis_web" / "jarvis-os.html"
    assert _jarvis_web_path("jarvis-os.html").exists()


def test_brain_server_jarvis_os_route_uses_static_file_serving():
    source = (ROOT / "src" / "openjarvis" / "cli" / "brain_server.py").read_text(
        encoding="utf-8"
    )

    route_start = source.index('"/jarvis-os"')
    route_block = source[route_start : route_start + 700]

    assert '"/jarvis-os.html"' in route_block
    assert 'self.path = "/jarvis-os.html"' in route_block
    assert "super().do_GET()" in route_block
```

- [ ] **Step 2: Run the focused test to verify it fails**

Run:

```powershell
& '.venv\Scripts\python.exe' -B -m pytest tests\web\test_jarvis_os.py -q
```

Expected: failures because `jarvis-os.html`, `ob-jarvis-os-btn`, and route handling do not exist.

- [ ] **Step 3: Add bootstrap static page**

Create `jarvis_web/jarvis-os.html` with:

```html
<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Jarvis OS</title>
</head>
<body>
  <main id="jarvis-os-root">
    <h1>Jarvis OS</h1>
  </main>
</body>
</html>
```

- [ ] **Step 4: Wire `/jarvis-os` static routing**

In `src/openjarvis/cli/brain_server.py`, update the static shell route tuple around the existing `/brain.html`, `/phone.html`, and `/markets.html` handling:

```python
        if path_only in ("/", "/brain", "/brain.html", "/phone", "/phone.html",
                          "/markets", "/markets.html",
                          "/jarvis-os", "/jarvis-os.html",
                          "/manifest.webmanifest"):
            # Static shell â€” auth happens client-side via the modal
            if path_only in ("/", "/brain", "/brain.html"):
                self.path = "/brain.html"
            elif path_only in ("/phone", "/phone.html"):
                self.path = "/phone.html"
            elif path_only in ("/markets", "/markets.html"):
                self.path = "/markets.html"
            elif path_only in ("/jarvis-os", "/jarvis-os.html"):
                self.path = "/jarvis-os.html"
            return super().do_GET()
```

Also add this fallback near the later static fallback block that currently handles `/brain` and `/phone`:

```python
        elif self.path in ("/jarvis-os", "/jarvis-os.html"):
            self.path = "/jarvis-os.html"
            super().do_GET()
```

- [ ] **Step 5: Add Operations Center launch button**

In `jarvis_web/brain.html`, find the top Operations Center banner actions near the existing chat/log/briefing/markets/TikTok buttons. Add:

```html
<button id="ob-jarvis-os-btn" class="ob-chat-btn" title="Open Jarvis OS" onclick="window.location='/jarvis-os'">Jarvis OS</button>
```

If the button text is too wide for `.ob-chat-btn`, add a dedicated CSS rule near the existing `#ops-banner .ob-chat-btn` styles:

```css
#ops-banner #ob-jarvis-os-btn {
  width: auto;
  min-width: 74px;
  padding: 0 9px;
  font-size: 10px;
  letter-spacing: 0.6px;
  text-transform: uppercase;
}
```

- [ ] **Step 6: Run the focused test to verify it passes**

Run:

```powershell
& '.venv\Scripts\python.exe' -B -m pytest tests\web\test_jarvis_os.py -q
```

Expected: `3 passed`.

- [ ] **Step 7: Commit Task 1**

Run:

```powershell
git add src\openjarvis\cli\brain_server.py jarvis_web\brain.html jarvis_web\jarvis-os.html tests\web\test_jarvis_os.py
git commit -m "feat(jarvis-os): add operations center launcher"
```

---

### Task 2: Jarvis OS State Endpoint

**Files:**
- Modify: `src/openjarvis/cli/brain_server.py`
- Modify: `tests/web/test_jarvis_os.py`

- [ ] **Step 1: Add failing state helper tests**

Append to `tests/web/test_jarvis_os.py`:

```python
def test_jarvis_os_state_shape_is_local_qwen_first():
    from openjarvis.cli.brain_server import _jarvis_os_state

    state = _jarvis_os_state()

    assert state["model"]["primary"] == "qwen3.6:27b"
    assert state["model"]["mode"] == "local-first"
    assert state["model"]["escalation"] == "Claude/Codex standby"
    assert set(state["widgets"]) >= {
        "missions",
        "agents",
        "plugins",
        "markets",
        "gpu",
        "schedule",
        "inbox",
        "memory",
    }
    assert isinstance(state["actions"], list)
    assert "New Mission" in state["actions"]


def test_brain_server_exposes_jarvis_os_state_endpoint():
    source = (ROOT / "src" / "openjarvis" / "cli" / "brain_server.py").read_text(
        encoding="utf-8"
    )

    assert 'elif self.path == "/jarvis-os/state":' in source
    assert "_jarvis_os_state()" in source
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```powershell
& '.venv\Scripts\python.exe' -B -m pytest tests\web\test_jarvis_os.py -q
```

Expected: failure because `_jarvis_os_state` and `/jarvis-os/state` do not exist.

- [ ] **Step 3: Add `_jarvis_os_state()` helper**

In `src/openjarvis/cli/brain_server.py`, add this helper near the other page helper functions, above `class _Handler`:

```python
def _jarvis_os_state() -> Dict[str, Any]:
    """Assemble the first Jarvis OS desktop payload.

    The first build intentionally returns stable, conservative widget
    contracts. Where live data is unavailable or expensive, the widget marks
    itself as "pending" rather than triggering heavy work on page load.
    """
    now = time.strftime("%H:%M")
    widgets: Dict[str, Any] = {
        "missions": {"label": "Active missions", "value": 0, "status": "pending"},
        "agents": {"label": "Agent queue", "value": 0, "status": "pending"},
        "plugins": {"label": "Plugin learning queue", "value": "Ready", "status": "ready"},
        "markets": {"label": "Market pulse", "value": "Idle", "status": "pending"},
        "gpu": {"label": "GPU / local load", "value": "Local", "status": "pending"},
        "schedule": {"label": "Scheduled work", "value": 0, "status": "pending"},
        "inbox": {"label": "Approvals / escalations", "value": 0, "status": "ready"},
        "memory": {"label": "Brain memory", "value": "Loaded", "status": "ready"},
    }
    try:
        snapshot = orch_bridge.get_snapshot()
        tasks = snapshot.get("tasks") or []
        agents = snapshot.get("agents") or []
        active_tasks = [
            t for t in tasks
            if (t.get("status") or "").lower() in {"running", "queued", "pending"}
        ]
        widgets["missions"]["value"] = len(active_tasks)
        widgets["missions"]["status"] = "ready"
        widgets["agents"]["value"] = len(agents)
        widgets["agents"]["status"] = "ready"
    except Exception:
        logger.debug("jarvis os orch snapshot unavailable", exc_info=True)
    try:
        from openjarvis.tools import agent_runner
        schedules = agent_runner.list_scheduled() or []
        widgets["schedule"]["value"] = len(schedules)
        widgets["schedule"]["status"] = "ready"
    except Exception:
        logger.debug("jarvis os schedule snapshot unavailable", exc_info=True)
    return {
        "ok": True,
        "time": now,
        "model": {
            "primary": "qwen3.6:27b",
            "alias": "qwen3.6-27b-local",
            "mode": "local-first",
            "escalation": "Claude/Codex standby",
        },
        "widgets": widgets,
        "actions": [
            "New Mission",
            "New Project",
            "Plugin Studio",
            "Model Center",
            "Memory",
            "Settings",
        ],
    }
```

- [ ] **Step 4: Wire `/jarvis-os/state`**

In `_dispatch_get`, add this before the generic static fallback:

```python
        elif self.path == "/jarvis-os/state":
            self._json_response(200, _jarvis_os_state())
```

- [ ] **Step 5: Run focused tests**

Run:

```powershell
& '.venv\Scripts\python.exe' -B -m pytest tests\web\test_jarvis_os.py -q
```

Expected: all tests in the file pass.

- [ ] **Step 6: Commit Task 2**

Run:

```powershell
git add src\openjarvis\cli\brain_server.py tests\web\test_jarvis_os.py
git commit -m "feat(jarvis-os): expose desktop state"
```

---

### Task 3: Desktop Widget Shell UI

**Files:**
- Modify: `jarvis_web/jarvis-os.html`
- Modify: `tests/web/test_jarvis_os.py`

- [ ] **Step 1: Add failing static UI tests**

Append to `tests/web/test_jarvis_os.py`:

```python
def test_jarvis_os_page_contains_desktop_shell_landmarks():
    html = (ROOT / "jarvis_web" / "jarvis-os.html").read_text(encoding="utf-8")

    for marker in (
        'id="jarvis-os-root"',
        'class="desktop-shell"',
        'class="desktop-shortcuts"',
        'id="start-menu"',
        'class="taskbar"',
        'id="mission-window"',
        'id="widget-grid"',
        'fetch("/jarvis-os/state")',
    ):
        assert marker in html


def test_jarvis_os_page_declares_required_widget_targets():
    html = (ROOT / "jarvis_web" / "jarvis-os.html").read_text(encoding="utf-8")

    for widget_id in (
        "widget-model",
        "widget-missions",
        "widget-agents",
        "widget-plugins",
        "widget-markets",
        "widget-gpu",
        "widget-schedule",
        "widget-inbox",
        "widget-memory",
    ):
        assert f'id="{widget_id}"' in html


def test_jarvis_os_page_uses_safe_text_assignment_for_state():
    html = (ROOT / "jarvis_web" / "jarvis-os.html").read_text(encoding="utf-8")

    assert "function setText(id, value)" in html
    assert ".textContent = value == null ? '' : String(value)" in html
    assert "innerHTML = state" not in html
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```powershell
& '.venv\Scripts\python.exe' -B -m pytest tests\web\test_jarvis_os.py -q
```

Expected: failures because the bootstrap page does not yet contain the shell.

- [ ] **Step 3: Replace bootstrap page with the desktop shell page**

Replace `jarvis_web/jarvis-os.html` with a complete standalone page containing:

```html
<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Jarvis OS</title>
  <style>
    :root {
      color-scheme: dark;
      --bg-a: #0d141d;
      --bg-b: #14202b;
      --bg-c: #14312d;
      --panel: rgba(15, 23, 42, 0.84);
      --panel-strong: rgba(8, 17, 31, 0.9);
      --line: rgba(255,255,255,0.16);
      --text: #e7eef7;
      --muted: #9fb2c6;
      --accent: #8ee7c6;
      --blue: #38bdf8;
    }
    * { box-sizing: border-box; }
    html, body { height: 100%; margin: 0; }
    body {
      font-family: Inter, "Segoe UI", system-ui, sans-serif;
      background: #050a12;
      color: var(--text);
      overflow: hidden;
    }
    button, input { font: inherit; }
    .desktop-shell {
      min-height: 100vh;
      padding: 24px;
      display: grid;
      grid-template-rows: 1fr auto;
      gap: 16px;
      background:
        radial-gradient(circle at 50% 0%, rgba(123,97,255,0.38), transparent 38%),
        linear-gradient(135deg, var(--bg-a), var(--bg-b) 46%, var(--bg-c));
      overflow: hidden;
    }
    .desktop-canvas {
      position: relative;
      min-height: 0;
      border-radius: 24px;
      overflow: hidden;
    }
    .desktop-shortcuts {
      position: absolute;
      top: 8px;
      left: 8px;
      display: grid;
      grid-template-columns: repeat(2, 82px);
      gap: 16px 12px;
      z-index: 5;
    }
    .shortcut {
      appearance: none;
      border: 0;
      color: var(--text);
      background: transparent;
      display: grid;
      gap: 7px;
      justify-items: center;
      cursor: pointer;
    }
    .shortcut-icon {
      width: 54px;
      height: 54px;
      border-radius: 16px;
      display: grid;
      place-items: center;
      background: rgba(255,255,255,0.12);
      border: 1px solid rgba(255,255,255,0.22);
      box-shadow: 0 14px 36px rgba(0,0,0,0.38);
    }
    .ambient-clock {
      position: absolute;
      left: 130px;
      top: 34px;
      font-size: clamp(48px, 8vw, 104px);
      font-weight: 300;
      color: rgba(231,238,247,0.34);
      letter-spacing: 0;
      pointer-events: none;
    }
    .widget-grid {
      position: absolute;
      top: 34px;
      right: 22px;
      width: min(430px, calc(100vw - 330px));
      display: grid;
      grid-template-columns: repeat(2, minmax(150px, 1fr));
      gap: 12px;
      z-index: 4;
    }
    .widget {
      min-height: 94px;
      padding: 13px 14px;
      border-radius: 18px;
      background: rgba(8, 17, 31, 0.72);
      border: 1px solid var(--line);
      box-shadow: 0 18px 48px rgba(0,0,0,0.34);
      backdrop-filter: blur(20px);
    }
    .widget .label {
      font-size: 11px;
      color: var(--accent);
      letter-spacing: 0.08em;
      text-transform: uppercase;
    }
    .widget .value {
      margin-top: 8px;
      font-size: 24px;
      font-weight: 700;
    }
    .widget .sub {
      margin-top: 4px;
      color: var(--muted);
      font-size: 12px;
    }
    .app-window {
      position: absolute;
      left: min(36vw, 420px);
      bottom: 74px;
      width: min(620px, 52vw);
      min-height: 310px;
      border-radius: 18px;
      overflow: hidden;
      background: var(--panel);
      border: 1px solid var(--line);
      box-shadow: 0 28px 90px rgba(0,0,0,0.55);
      backdrop-filter: blur(22px);
      z-index: 6;
    }
    .window-titlebar {
      height: 46px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 0 15px;
      background: rgba(17, 28, 45, 0.82);
      border-bottom: 1px solid rgba(255,255,255,0.1);
    }
    .window-body { padding: 16px; }
    .tabs { display: flex; gap: 8px; flex-wrap: wrap; }
    .chip {
      border: 1px solid rgba(142,231,198,0.24);
      background: rgba(142,231,198,0.08);
      color: var(--text);
      border-radius: 999px;
      padding: 7px 11px;
    }
    .transcript {
      margin-top: 14px;
      height: 156px;
      border-radius: 14px;
      padding: 12px;
      background: var(--panel-strong);
      border: 1px solid rgba(255,255,255,0.1);
      color: #cbd5e1;
      overflow: auto;
      line-height: 1.55;
    }
    .command-row { margin-top: 12px; display: flex; gap: 8px; }
    .command-row input {
      flex: 1;
      min-width: 0;
      border: 1px solid rgba(255,255,255,0.16);
      background: rgba(8,17,31,0.82);
      color: var(--text);
      border-radius: 13px;
      padding: 10px 12px;
    }
    .command-row button, .start-menu button, .taskbar button {
      border: 1px solid rgba(255,255,255,0.16);
      background: rgba(255,255,255,0.10);
      color: var(--text);
      border-radius: 12px;
      padding: 9px 12px;
      cursor: pointer;
    }
    .start-menu {
      position: absolute;
      left: 50%;
      bottom: 82px;
      transform: translateX(-50%);
      width: min(420px, calc(100vw - 32px));
      display: none;
      grid-template-columns: repeat(2, 1fr);
      gap: 10px;
      padding: 14px;
      background: rgba(15, 23, 42, 0.88);
      border: 1px solid var(--line);
      box-shadow: 0 24px 80px rgba(0,0,0,0.58);
      border-radius: 20px;
      backdrop-filter: blur(22px);
      z-index: 10;
    }
    .start-menu.open { display: grid; }
    .taskbar {
      height: 62px;
      justify-self: center;
      width: min(760px, calc(100vw - 32px));
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 10px;
      padding: 0 16px;
      border-radius: 21px;
      background: rgba(11,18,32,0.88);
      border: 1px solid rgba(255,255,255,0.16);
      box-shadow: 0 20px 58px rgba(0,0,0,0.56);
      backdrop-filter: blur(22px);
    }
    .tray {
      margin-left: 10px;
      color: var(--muted);
      font-size: 12px;
      white-space: nowrap;
    }
    @media (max-width: 820px) {
      body { overflow: auto; }
      .desktop-shell { min-height: 100dvh; padding: 12px; overflow: visible; }
      .desktop-canvas { min-height: 980px; }
      .desktop-shortcuts { grid-template-columns: repeat(4, 72px); }
      .ambient-clock { top: 128px; left: 18px; font-size: 48px; }
      .widget-grid {
        top: 210px;
        left: 0;
        right: 0;
        width: auto;
        grid-template-columns: 1fr;
      }
      .app-window {
        left: 0;
        right: 0;
        bottom: 86px;
        width: auto;
      }
      .taskbar { position: sticky; bottom: 10px; overflow-x: auto; justify-content: flex-start; }
    }
  </style>
</head>
<body>
  <main id="jarvis-os-root" class="desktop-shell">
    <section class="desktop-canvas" aria-label="Jarvis OS desktop">
      <div class="desktop-shortcuts">
        <button class="shortcut" data-open="mission"><span class="shortcut-icon">J</span><span>Jarvis</span></button>
        <button class="shortcut" data-open="mission"><span class="shortcut-icon">Run</span><span>Missions</span></button>
        <button class="shortcut" data-open="projects"><span class="shortcut-icon">Dev</span><span>Projects</span></button>
        <button class="shortcut" data-open="plugins"><span class="shortcut-icon">Add</span><span>Plugins</span></button>
        <button class="shortcut" data-open="memory"><span class="shortcut-icon">Mem</span><span>Memory</span></button>
        <button class="shortcut" data-open="models"><span class="shortcut-icon">AI</span><span>Models</span></button>
      </div>

      <div id="desktop-clock" class="ambient-clock">--:--</div>

      <section id="widget-grid" class="widget-grid" aria-label="Jarvis OS widgets">
        <article id="widget-model" class="widget"><div class="label">Local Model</div><div id="model-value" class="value">Qwen</div><div id="model-sub" class="sub">Loading</div></article>
        <article id="widget-missions" class="widget"><div class="label">Missions</div><div id="missions-value" class="value">--</div><div id="missions-sub" class="sub">Active work</div></article>
        <article id="widget-agents" class="widget"><div class="label">Agents</div><div id="agents-value" class="value">--</div><div id="agents-sub" class="sub">Queue</div></article>
        <article id="widget-plugins" class="widget"><div class="label">Plugins</div><div id="plugins-value" class="value">--</div><div id="plugins-sub" class="sub">Learning queue</div></article>
        <article id="widget-markets" class="widget"><div class="label">Markets</div><div id="markets-value" class="value">--</div><div id="markets-sub" class="sub">Pulse</div></article>
        <article id="widget-gpu" class="widget"><div class="label">GPU</div><div id="gpu-value" class="value">--</div><div id="gpu-sub" class="sub">Local load</div></article>
        <article id="widget-schedule" class="widget"><div class="label">Calendar</div><div id="schedule-value" class="value">--</div><div id="schedule-sub" class="sub">Scheduled work</div></article>
        <article id="widget-inbox" class="widget"><div class="label">Inbox</div><div id="inbox-value" class="value">--</div><div id="inbox-sub" class="sub">Approvals</div></article>
        <article id="widget-memory" class="widget"><div class="label">Memory</div><div id="memory-value" class="value">--</div><div id="memory-sub" class="sub">Brain recall</div></article>
      </section>

      <section id="mission-window" class="app-window" aria-label="Mission Session">
        <div class="window-titlebar"><strong>Mission Session</strong><span id="mission-model">local Qwen</span></div>
        <div class="window-body">
          <div class="tabs"><button class="chip">Chat</button><button class="chip">Plan</button><button class="chip">Files</button><button class="chip">Tools</button><button class="chip">Logs</button></div>
          <div class="transcript">Jarvis OS is ready. Start a mission, open a project, create a plugin, or inspect memory. Local Qwen 3.6 27B is the default engine.</div>
          <div class="command-row"><input id="mission-input" aria-label="Create a mission for Jarvis OS"><button type="button">Start</button></div>
        </div>
      </section>

      <nav id="start-menu" class="start-menu" aria-label="Jarvis OS Start menu">
        <button type="button">New Mission</button>
        <button type="button">New Project</button>
        <button type="button">Plugin Studio</button>
        <button type="button">Model Center</button>
        <button type="button">Memory</button>
        <button type="button">Settings</button>
      </nav>
    </section>

    <footer class="taskbar" aria-label="Jarvis OS taskbar">
      <button id="start-button" type="button">Start</button>
      <button type="button" data-open="mission">Mission</button>
      <button type="button" data-open="operations">Operations</button>
      <button type="button" data-open="plugins">Plugins</button>
      <button type="button" data-open="memory">Memory</button>
      <button type="button" data-open="models">Models</button>
      <span id="tray-status" class="tray">Qwen 3.6 27B Â· Local-first</span>
    </footer>
  </main>

  <script>
    function setText(id, value) {
      const node = document.getElementById(id);
      if (node) node.textContent = value == null ? '' : String(value);
    }
    function setWidget(name, widget) {
      if (!widget) return;
      setText(name + '-value', widget.value);
      setText(name + '-sub', widget.label + ' Â· ' + widget.status);
    }
    function updateClock(value) {
      setText('desktop-clock', value || new Date().toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' }));
    }
    async function fetchState() {
      try {
        const res = await fetch("/jarvis-os/state");
        const state = await res.json();
        updateClock(state.time);
        setText('model-value', state.model && state.model.primary);
        setText('model-sub', state.model && (state.model.mode + ' Â· ' + state.model.escalation));
        setText('mission-model', state.model && state.model.alias);
        setText('tray-status', (state.model && state.model.primary ? state.model.primary : 'Qwen') + ' Â· Local-first');
        const widgets = state.widgets || {};
        setWidget('missions', widgets.missions);
        setWidget('agents', widgets.agents);
        setWidget('plugins', widgets.plugins);
        setWidget('markets', widgets.markets);
        setWidget('gpu', widgets.gpu);
        setWidget('schedule', widgets.schedule);
        setWidget('inbox', widgets.inbox);
        setWidget('memory', widgets.memory);
      } catch (err) {
        updateClock();
        setText('tray-status', 'Jarvis OS state offline');
      }
    }
    document.getElementById('start-button').addEventListener('click', () => {
      document.getElementById('start-menu').classList.toggle('open');
    });
    document.addEventListener('click', (event) => {
      if (!event.target.closest('#start-menu') && !event.target.closest('#start-button')) {
        document.getElementById('start-menu').classList.remove('open');
      }
    });
    fetchState();
    setInterval(fetchState, 10000);
  </script>
</body>
</html>
```

- [ ] **Step 4: Run static UI tests**

Run:

```powershell
& '.venv\Scripts\python.exe' -B -m pytest tests\web\test_jarvis_os.py -q
```

Expected: all tests pass.

- [ ] **Step 5: Commit Task 3**

Run:

```powershell
git add jarvis_web\jarvis-os.html tests\web\test_jarvis_os.py
git commit -m "feat(jarvis-os): build desktop widget shell"
```

---

### Task 4: Browser Verification And Mobile Guard

**Files:**
- Modify only if verification reveals layout failures:
  - `jarvis_web/jarvis-os.html`
  - `jarvis_web/brain.html`
- Test: `tests/web/test_jarvis_os.py`

- [ ] **Step 1: Run server-side and static verification**

Run:

```powershell
& '.venv\Scripts\python.exe' -B -m pytest tests\web\test_jarvis_os.py tests\tiktok\test_web_dashboard.py -q
```

Expected: Jarvis OS tests pass and TikTok/Operations Center dashboard tests still pass.

- [ ] **Step 2: Run Python compile check**

Run:

```powershell
& '.venv\Scripts\python.exe' -B -m py_compile src\openjarvis\cli\brain_server.py
```

Expected: no output and exit code 0.

- [ ] **Step 3: Start or reuse Jarvis server for browser checks**

If Jarvis is already running on port 7710, use it. Otherwise run the existing launcher or a local server path used by this project. Do not edit `jarvis.bat`.

Manual URLs:

```text
http://127.0.0.1:7710/brain.html
http://127.0.0.1:7710/jarvis-os
http://127.0.0.1:7710/jarvis-os/state
```

Expected:

- `/brain.html` loads and shows a `Jarvis OS` launcher.
- `/jarvis-os` loads the desktop widget shell.
- `/jarvis-os/state` returns JSON with `"primary": "qwen3.6:27b"`.

- [ ] **Step 4: Verify browser layout at desktop and mobile sizes**

Use browser screenshots or Playwright if available. Check these viewports:

```text
1440x900
390x844
320x568
```

Expected:

- desktop shortcuts remain clickable
- taskbar does not overlap command input
- Start menu opens and fits on screen
- widgets do not cover the Mission Session on mobile
- no horizontal scrolling on mobile
- Operations Center top banner still fits after adding the `Jarvis OS` button

- [ ] **Step 5: Fix any concrete layout failure**

If mobile widgets overlap the Mission Session, modify the mobile CSS in `jarvis_web/jarvis-os.html` to increase `.desktop-canvas` height:

```css
@media (max-width: 820px) {
  .desktop-canvas { min-height: 1120px; }
}
```

If the Operations Center banner overflows on small screens, modify the existing mobile banner rule in `jarvis_web/brain.html` to hide the text button on very small widths:

```css
@media (max-width: 380px) {
  #ops-banner #ob-jarvis-os-btn {
    min-width: 34px;
    width: 34px;
    padding: 0;
    font-size: 0;
  }
  #ops-banner #ob-jarvis-os-btn::before {
    content: "OS";
    font-size: 10px;
  }
}
```

- [ ] **Step 6: Re-run verification after fixes**

Run:

```powershell
& '.venv\Scripts\python.exe' -B -m pytest tests\web\test_jarvis_os.py tests\tiktok\test_web_dashboard.py -q
& '.venv\Scripts\python.exe' -B -m py_compile src\openjarvis\cli\brain_server.py
git diff --check
```

Expected:

- tests pass
- compile check passes
- `git diff --check` reports no errors

- [ ] **Step 7: Commit Task 4 if files changed**

If Step 5 required edits, commit them:

```powershell
git add jarvis_web\jarvis-os.html jarvis_web\brain.html tests\web\test_jarvis_os.py
git commit -m "fix(jarvis-os): harden desktop shell layout"
```

If no files changed, do not create an empty commit.

---

## Final Verification

Run:

```powershell
git status --short
& '.venv\Scripts\python.exe' -B -m pytest tests\web\test_jarvis_os.py tests\tiktok\test_web_dashboard.py -q
& '.venv\Scripts\python.exe' -B -m py_compile src\openjarvis\cli\brain_server.py
git diff --check
```

Expected:

- only unrelated pre-existing untracked files remain, currently `src/openjarvis/tools/vault_stats.py` and `tests/tools/test_vault_stats.py`
- focused tests pass
- Python compile check passes
- diff check passes

Restart `jarvis.bat` before assuming the live Operations Center has picked up the new static page and server route.


